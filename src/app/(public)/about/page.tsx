import AboutClient from "./AboutClient";

export const metadata = {
  title: "Our Story | Buildora",
  description: "We are a collective of designers and engineers bringing soul back to the web.",
};

export default function AboutPage() {
  return <AboutClient />;
}
